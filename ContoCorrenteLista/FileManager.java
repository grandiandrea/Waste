import java.util.*;
public class FileManager{
	Path buffer;
	Path registro;
	public FileManager(){
		this.buffer = Paths.get("buffer.txt");
	}
	public Write(FileWrapper contenuto){
		List<FileWrapper> lista = new ArrayList<>();
		lista.add(contenuto);
	}
}